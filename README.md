# LRU Cache API with TTL

REST API implementation for an LRU Cache with TTL support.

## Requirements

- Docker
- Docker Compose

## Running the Application

1. Build and start the container:
```bash
docker-compose up --build
```

## Testing
### To run tests:
```bash
docker-compose run web pytest tests/
```
